//@include "tn/system/trace.jsx";var selection = activeDocument.selection;var isFixedRatio = true;var isChangePositions = true;var isChangeFillPatterns = false;var isChangeFillGradients = false;var isChangeStrokePattern = false;var isChangeLineWidths = true;var baseScaleX = 10;var baseScaleY = 10;var maxScaleX = 200;var maxScaleY = 200;if(selection != ""){  for(var i=0; i<selection.length; i++) {    var obj = selection[i];    var scaleY;        var scaleX = Math.random() * maxScaleX + baseScaleX;        if(isFixedRatio){      scaleY = scaleX;    }else{      scaleY = Math.random() * maxScaleY + baseScaleY;    }    obj.resize(scaleX, scaleY, isChangePositions, isChangeFillPatterns, isChangeFillGradients, isChangeStrokePattern, isChangeLineWidths, Transformation.CENTER);  }}else{  trace("not select");}