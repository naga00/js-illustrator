//@include "tn/system/trace.jsx";var selection = activeDocument.selection;var xmax = activeDocument.width;var ymax = activeDocument.height;var selectionCount = 0;var duplicateMax = 10;var duplicateCount = 0;function duplicate(){  if(duplicateCount < duplicateMax){    var obj = selection[selectionCount];    var dupObj = obj.duplicate();    dupObj.selected = false;         var x = Math.random() * (xmax - dupObj.width);    var y = - Math.random() * (ymax - dupObj.height);    dupObj.position = [x, y];    if(selectionCount >= selection.length - 1){      selectionCount = 0;    }else{      selectionCount += 1;    }    duplicateCount += 1;     duplicate();  }}  if(selection != ""){  duplicate();}else{  trace("not select");}