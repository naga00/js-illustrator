//@include "tn/system/trace.jsx";//@include "tn/color/ColorEx.jsx";var selection = activeDocument.selection;var colorType = "HSV";var isOpacity = false;if(selection != ""){  for(var i=0; i<selection.length; i++) {    var myColor;    if(colorType == "RGB"){      myColor = ColorEx.randomRGBColor(255, 255, 255);    }else if(colorType == "CMYK"){      myColor = ColorEx.randomCMYKColor(100, 100, 100, 100);    }else if(colorType == "GRAY"){      myColor = ColorEx.randomGRAYColor(100);    }else if(colorType == "HSV"){      myColor = ColorEx.randomHSBColor(360, 100, 100);    }else{      myColor = ColorEx.randomRGBColor(255, 255, 255);    }      var obj = selection[i];    obj.fillColor = myColor;    if(isOpacity){      obj.opacity = Math.random() * 100;    }  }}else{  trace("not select");}