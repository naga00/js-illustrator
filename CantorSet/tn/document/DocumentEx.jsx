//@include "DocumentPresetEx.jsx";function DocumentEx(isNewDocument, documentPreset) {	isNewDocument = (isNewDocument != undefined) ? isNewDocument : true;	if(isNewDocument){		document = documents.addDocument(documentPreset.colorMode, documentPreset);	}else{		if(app.documents.length > 0){			document = app.documents[0];		}else{			document = app.documents.addDocument(documentPreset.colorMode, documentPreset);		}	}	DocumentEx.initialize();	return document;}DocumentEx.initialize = function() {	document.layers.removeAll();		Document.prototype.addLayer = function(name) {		app.documents[0].layers.add();		app.documents[0].layers[0].name = name;	};};